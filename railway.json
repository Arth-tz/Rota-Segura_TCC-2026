{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildEnvironment": "V3"
  },
  "deploy": {
    "releaseCommand": "php artisan migrate --force && php artisan db:seed --force"
  }
}
